import React from 'react';

    const  Input = (name) => {
    return (
        <div className="input">
            <input />
            <label>{name}</label>
        </div>
    )
}

export default Input;